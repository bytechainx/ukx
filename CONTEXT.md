# ukx 上下文

本文件定义 `ukx` 与其使用方共享的核心词汇、能力边界与已知缺口。
它只记录领域含义与取舍，不复制 API 签名。

## 角色与边界

**源事实层**：把 `specs/adapter/uk_cb.md` 声明的源、码、路由禁则与调度落成可编译、
可判定、可测试的代码。它**不是**采集器，**不是**存储层，也**不是**派生指标层。
_Avoid_: 采集器 / 适配器运行时（本层无网络、无凭据、无调度执行）

**离线解析**：输入是**源标识 + 字符串**，输出是类型化观测集合。
参数里没有 URL、客户端、认证信息与环境变量。
_Avoid_: 客户端 / 传输层（解析不产生任何 I/O）

**离线交换形态**：本层自己声明的 CSV 与 JSON 形状（见 `docs/标准.md` §6），
**不是** BoE 的文件布局。BoE 的多格式栈（CSV/XML/Excel/xlsx/ZIP）未被引入。
_Avoid_: 源格式（把本层形态当成源格式会误判覆盖度）

## 源事实词汇

**源登记**（`UkCbSourcePlan` / `SOURCE_PLANS`）：S01–S11 的标识、名称与清单
「形式」「频率」两列的**逐字文本**。文本原样保留，因为清单给的是自由文本
（如「日/月/季」「周/操作日」），四舍五入成单一值会丢失信息。
_Avoid_: 采集配置（登记是只读事实，不是可执行配置）

**点名格式**（`UkCbFormat`）：只在清单**明确点名**时登记。
「形式」列为节奏者（S02 事件 / S06 日 T+1 / S08 日 / S09 月）格式数组为空 ⇒
离线解析返回 `NotApplicable`，**不猜**格式。
_Avoid_: 推断格式（猜格式等于编造源事实）

**关键 IADB 码**（`UkCbIadbCode` / `IADB_KEY_CODES`）：清单 §1.2 的 6 个
**规划白名单种子**。`iadb_code` 严格选择：不在种子内即 `SemanticallyRejected`。
_Avoid_: 全集 / 白名单（清单只给了种子，全集未钉死，**不得**自行扩集）

**序列标识**（`UkCbSeriesId`）：观测里原样保留的源侧标识文本（字符集受限）。
它**不**解释含义，只做保留与关键码命中判定。
_Avoid_: 码 / 符号（近义码不得互相顶替）

**曲线槽位**（`CurveRoute` / `CURVE_ROUTE`）：S07 收益率曲线的去向 ——
`yield_curve`（#11 `boe_iadb` / #12 `uk_dmo`）。本层**不**代理该槽位职责。
_Avoid_: 曲线源（本层是曲线的**拒绝方**，不是提供方）

**规划调度**（`PLANNED_SCHEDULE`）：清单 §1.4 的 8 条 UTC 窗口。
它是**规划语义**，不是访问合同，也不是抓取窗口。
_Avoid_: 采集时刻表（本层无采集能力）

## 判定词汇

**曲线路由优先**：任何解析之前先过 `guard_curve`；曲线源返回 `RoutedElsewhere`，
**优先于**「格式未实现」判定。顺序不可调换，否则曲线会被误报成能力缺口。
_Avoid_: 先格式后路由（会产生误导性的错误分类）

**具名缺失**（`UkCbValue::Absent`）：缺失由理由具名表达 ——
`BlankInSource`（源行为空）与 `ExplicitMarker`（源给出非数值标记）。
后者**不解释**其字面含义（清单未固定标记字面量）。
_Avoid_: 0 / null（缺失转 0 是静默错误；本层绝不这么做）

**fail-closed 授权**：证据缺失、范围不明、签署者不明、未获 Owner 签核、
尚未生效、已过期 —— 六条路径一律 `Denied`。
_Avoid_: 清单批准（清单登记的是现状，不是签核证据）

## 已落地与未落地

**已落地**：源登记、关键码与其守卫、曲线路由、规划调度、观测值对象与校验、
CSV / JSON 离线解析、授权判定、publication 语义、三类测试与合成夹具、微基准。

**未落地（不假装支持）**：

1. S01–S11 的传输层与采集（无授权，`FR-037`）；
2. xlsx / ZIP / XML / Excel 解析栈；
3. 「形式」列未点名格式的源（S02 / S06 / S08 / S09）的离线解析；
4. 官方 vintage / 发布日历 —— 故 PIT 资格恒 `NotEligible`。

## rust-version 推导

规则：`rust-version` = 依赖图中所有依赖所声明 `rust_version` 的**最大值**
（`FR-014` 的可核验化，见 `contracts/source-library-contract.md` §7.3）。

`cargo metadata --format-version 1` 实读结果（2026-09-22）：

| 依赖 | 版本 | 其 `rust_version` |
| --- | --- | --- |
| `thiserror` | 2.0.20 | 1.71 |
| `thiserror-impl` | 2.0.20 | 1.71 |
| `serde` | 1.0.229 | 1.56 |
| `serde_core` | 1.0.229 | 1.56 |
| `serde_derive` | 1.0.229 | 1.71 |
| `serde_json` | 1.0.151 | 1.71 |
| `proc-macro2` | 1.0.107 | 1.71 |
| `quote` | 1.0.47 | 1.71 |
| `syn` | 3.0.6 | 1.71 |
| `unicode-ident` | 1.0.26 | 1.71 |
| `memchr` | 2.8.3 | 1.61 |
| `itoa` | 1.0.18 | 1.68 |

**推导结果**：`1.71`（最大值取自 `thiserror` / `serde_derive` / `serde_json` 等）。
语言特性方面本 crate 只用到 edition 2021 与标准库，未额外抬高该下界。

## 已知缺口

1. **夹具全为合成样本**：来自上游的真实 fixture 已随重写删除，本层无真实样本可用；
   因此解析器只接受显式标注（`_synthetic: true` + 非空 `_note`）的样本。
2. **多格式未落地**：xlsx / ZIP / XML / Excel 解析栈未引入，调用方会得到
   `NotApplicable` —— 这是**设计**，不是待办。
3. **CSV 子集**：本层只支持逗号分隔、无引号、无嵌入换行与逗号的子集；
   含引号者直接拒绝（宁拒绝、不误解析）。
4. **不解释空值标记**：`ExplicitMarker` 只表示「源给出了非数值标记」，
   不映射到任何具体业务语义。
5. **不可用于生产**：`production_decision = NO-GO`，且授权判定在真实调用上返回 `Denied`。
